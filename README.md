# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript and enable type-aware lint rules. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
Components Started
1. Backend (Django)
Directory: backend/
Command: python manage.py runserver
Status: Running at http://localhost:8000/
2. Frontend (React)
Directory: frontend/
Command: npm start
Status: Running at http://localhost:3000/
Verification
Frontend URL: http://localhost:3000/
Confirmation: The browser correctly loads the "Resume Craft AI" sign-up page.
Backend URL: http://localhost:8000/
Confirmation: Django server is active and responding.
Frontend Running (Note: I haven't actually saved a screenshot file yet, but I can describe it or skip for now if I can't easily capture one after subagent cancellation. Actually, the metadata shows the page is active.)